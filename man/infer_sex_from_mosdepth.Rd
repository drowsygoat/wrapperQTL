% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infer_chicken_sex_from_mosdepth.R
\name{infer_sex_from_mosdepth}
\alias{infer_sex_from_mosdepth}
\title{Infer Sex (ZW system) from mosdepth summaries and optionally write outputs}
\usage{
infer_sex_from_mosdepth(
  mosdepth_dir,
  out_dir = ".",
  threshold = 1.5,
  chrom_labels = c("Z", "W"),
  verbose = TRUE
)
}
\arguments{
\item{mosdepth_dir}{Character. Path to a directory containing mosdepth outputs.}

\item{out_dir}{Character. Directory where output files (\code{covariates_sex.txt} and
\code{ZW_coverage_ratio.pdf}) will be written. Default: current working directory.}

\item{threshold}{Numeric. Z:W ratio cutoff (default \code{1.5}).}

\item{chrom_labels}{Character vector of length 2 or named list with \code{"Z"} and \code{"W"}.}

\item{verbose}{Logical. Print messages (default \code{TRUE}).}
}
\value{
A list with:
\itemize{
\item \code{results}: data.frame with per-sample Z, W, ratio, and sex call.
\item \code{covariates}: one-row data.frame suitable for Matrix eQTL.
\item \code{files}: character vector of summary files used.
\item \code{plot_file}: path to saved PDF (if written).
\item \code{covariate_file}: path to covariate file (if written).
}
}
\description{
Reads all \code{summary.txt} files under a mosdepth output directory, extracts mean
coverage for chromosomes Z and W, computes the Z:W coverage ratio per sample,
and calls sex under the avian ZW system (female = ZW, male = ZZ).
}
\details{
Infer biological sex from mosdepth Z and W chromosome coverage
}

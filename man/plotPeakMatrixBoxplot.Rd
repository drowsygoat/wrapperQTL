% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotPeakMatrixBoxplot.R
\name{plotPeakMatrixBoxplot}
\alias{plotPeakMatrixBoxplot}
\title{Plot PeakMatrix Boxplots per Sample}
\usage{
plotPeakMatrixBoxplot(
  se,
  assay_name = "PeakMatrix",
  top_n_peaks = NULL,
  title = NULL,
  outfile = NULL,
  lim_y = 1
)
}
\arguments{
\item{se}{A \code{SummarizedExperiment} containing the peak signal assay.}

\item{assay_name}{Name of the assay to use. Default is \code{"PeakMatrix"}.}

\item{top_n_peaks}{Optional integer. If set, selects the top N peaks
(by signal) per sample before plotting. The function will return
the subsetted \code{SummarizedExperiment} invisibly in this case.}

\item{title}{Optional plot title. If \code{NULL}, a default title is generated.}

\item{outfile}{Optional file path. If provided, saves the plot as a high-resolution PDF.}

\item{lim_y}{Numeric; upper y-axis limit for plotting. Default is \code{1}.}
}
\value{
\itemize{
\item If \code{top_n_peaks} is provided: the subsetted \code{SummarizedExperiment} (invisibly).
\item If \code{top_n_peaks} is \code{NULL}: \code{NULL} (invisibly).
}
}
\description{
Generates a per-sample boxplot of peak signals from a \code{SummarizedExperiment} object.
Optionally subsets to the top N peaks per sample before plotting.
Can save the plot to a PDF and optionally return the subsetted object.
}
\details{
The function:
\enumerate{
\item Validates that the input is a \code{SummarizedExperiment} with the specified assay.
\item Optionally subsets to the top N peaks per sample.
\item Converts the peak matrix to a long format (supports dense and sparse matrices).
\item Creates a combined boxplot + quasi-random scatter plot of peak signals.
}

Sparse matrices (\code{dgCMatrix} or \code{dgTMatrix}) are converted internally for plotting.
Missing values are ignored by \code{ggplot2}.
}
\examples{
\dontrun{
library(SummarizedExperiment)

# Example SE object with assay "PeakMatrix"
plotPeakMatrixBoxplot(se, top_n_peaks = 1000, outfile = "peaks.pdf")
}

}
